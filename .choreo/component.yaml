name: video-processor-flask
projectName: your-project-name
componentType: webapp
build:
  buildpacks: []
  dockerfilePath: Dockerfile
  context: .
run:
  command: []
  env:
    - name: PORT
      value: "8080"
endpoints:
  - name: flask-endpoint
    port: 8080
    context: /
    exposure: public
    type: REST
